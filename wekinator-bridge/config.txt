# HandsfreeOSC to Wekinator Bridge Configuration
# Edit these to change where the bridge listens and sends data
WS_HOST=localhost
WS_PORT=8080
OSC_HOST=127.0.0.1
OSC_PORT=6448
OSC_ADDRESS=/wek/inputs
